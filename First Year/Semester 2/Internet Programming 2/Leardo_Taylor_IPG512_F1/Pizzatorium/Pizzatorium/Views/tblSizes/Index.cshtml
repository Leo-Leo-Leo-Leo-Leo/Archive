@model IEnumerable<Pizzatorium.Models.tblSize>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dSize)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dSize)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.dSizeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.dSizeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.dSizeID })
        </td>
    </tr>
}

</table>
